# Git 常用指令

## 初始設定
git init
git remote add origin https://github.com/yourname/dev-notes.git

## 常用指令
git status
git add .
git commit -m "說明"
git push origin main

## 分支操作
git checkout -b feature/new-ui
